<div class="page-header">
  <h1>
    My Favorites
    <a href="/oysters" class="btn btn-lg btn-success"><i class="fa fa-plus"></i></a>
  </h1>
</div>


<hr>
<h2>Issues on this page</h2>
  <p>1) Should only see current_user.favorites - but i'm seeing all users</p>
  <p>2) Need to restrict adding a favorite twice</p>
  <p>3) Cannot delete a favorite</p>

<div class="row isotope-container">
<% @favorites.each do |favorite| %>
  <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3 isotope-item">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Favorite #<%= favorite.oyster_id %></h3>
      </div>
      <div class="panel-body">
        <dl>
          <dt>User</dt>
          <dd><%= favorite.id %></dd>

          <dt>posted</dt>
          <dd><%= time_ago_in_words(favorite.created_at)%></dd>

          <dt>time stamp</dt>
          <dd><%= favorite.created_at.strftime("%D") %></dd>

          <dt>Delete favorite (NEED HELP)</dt>
          <div class="btn-group btn-group-right">
            <a href="/delete_favorites/<%= favorite.oyster_id %>" class="btn btn-info">
            <i class="fa fa-star"></i>
          </a>



        </dl>
      </div>
    </div>
  </div>
<% end %>
</div>




<!-- You can ignore the following. It's a quick and dirty example of how to get a Pinterest-style layout with quick search. -->
<!-- JavaScript for Isotope layout and quicksearch -->
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery.imagesloaded/3.1.8/imagesloaded.pkgd.min.js"></script>
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery.isotope/2.0.0/isotope.pkgd.min.js"></script>
<script>
  $(function() {
    // quick search regex
    var qsRegex;

    // init Isotope
    var $container = $('.isotope-container');

    $container.imagesLoaded(function() {
      $container.isotope({
        itemSelector: '.isotope-item',
        layoutMode: 'masonry',
        filter: function() {
          return qsRegex ? $(this).text().match( qsRegex ) : true;
        }
      })
    });

    // use value of search field to filter
    var $quicksearch = $('#quicksearch').keyup( debounce( function() {
      qsRegex = new RegExp( $quicksearch.val(), 'gi' );
      $container.isotope();
    }, 200 ) );

  });

  // debounce so filtering doesn't happen every millisecond
  function debounce( fn, threshold ) {
    var timeout;
    return function debounced() {
      if ( timeout ) {
        clearTimeout( timeout );
      }
      function delayed() {
        fn();
        timeout = null;
      }
      timeout = setTimeout( delayed, threshold || 100 );
    }
  }
</script>

<!-- End responsive, Isotope-based layout example -->

